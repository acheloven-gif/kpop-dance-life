# Развертывание K-Cover Dance Life в Telegram Bot

## Структура проекта

```
├── game/                 # React веб-приложение (Vite)
│   ├── src/
│   ├── dist/            # Собранное приложение
│   └── vite.config.ts
│
└── bot/                 # Telegram бот (Node.js + Telegraf)
    ├── src/
    ├── dist/            # Собранное приложение
    └── package.json
```

## Шаг 1: Создание Telegram бота

1. Откройте Telegram и напишите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например, `K-Cover Dance Life`)
   - Введите username (например, `kcd_game_bot`)
4. Скопируйте полученный токен
5. Сохраните токен в переменную окружения

## Шаг 2: Подготовка среды

### Локальная разработка

```bash
# В папке игры
cd game
npm install
npm run build

# В папке бота
cd ../bot
npm install
cp .env.example .env

# Отредактируйте .env файл
# TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN
# WEB_APP_URL=http://localhost:3000/game
# PORT=3000
```

### Production развертывание

Потребуется HTTPS сервер с доменом. Рекомендуемые хостинг-провайдеры:
- Railway.app
- Render.com
- Vercel + Express сервер
- AWS Lightsail
- DigitalOcean

## Шаг 3: Локальное тестирование

```bash
# Терминал 1: Запустить бот
cd bot
npm run dev

# Терминал 2: Запустить веб-приложение (опционально для отладки)
cd game
npm run dev
```

Бот будет доступен в Telegram, веб-приложение на `http://localhost:3000/game`

## Шаг 4: Production развертывание (Railway.app пример)

### 1. Подготовка репозитория

```bash
git init
git add .
git commit -m "Initial commit"
```

### 2. Развертывание на Railway

```bash
npm install -g @railway/cli
railway login
railway init
railway up
```

### 3. Настройка переменных окружения в Railway

В dashboard Railway установите:
```
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN
WEB_APP_URL=https://your-railway-url/game
NODE_ENV=production
WEBHOOK_URL=https://your-railway-url
PORT=3000
```

### 4. Обновление вебхука бота

После развертывания, бот автоматически установит вебхук на `https://your-railway-url/bot{TOKEN}`

## Шаг 5: Проверка

1. Откройте Telegram и найдите вашего бота
2. Отправьте `/start`
3. Нажмите кнопку "🎮 Play Game"
4. Должно открыться веб-приложение

## Структура URL в production

```
Бот: https://t.me/YOUR_BOT_USERNAME
Веб-приложение: https://your-domain.com/game
API сервер: https://your-domain.com (для вебхука)
```

## Переменные окружения

### Обязательные
- `TELEGRAM_BOT_TOKEN` - токен бота от BotFather
- `WEB_APP_URL` - URL веб-приложения (HTTPS для production)

### Опциональные
- `PORT` - порт сервера (по умолчанию 3000)
- `NODE_ENV` - `development` или `production`
- `WEBHOOK_URL` - URL для вебхука (только для production)
- `ADMIN_IDS` - ID администраторов для управления
- `DATABASE_URL` - Для сохранения статистики

## Сохранение данных игрока

По умолчанию данные сохраняются в Telegram CloudStorage:
- Автоматическое сохранение в локальное хранилище браузера
- Синхронизация с Telegram CloudStorage при наличии доступа
- Данные сохраняются отдельно для каждого пользователя

Для production с базой данных:
1. Установите PostgreSQL
2. Добавьте `DATABASE_URL` в .env
3. Добавьте обработчик в `bot/src/index.ts` для сохранения в БД

## Полезные команды

```bash
# Сборка
npm run build

# Разработка
npm run dev

# Проверка типов
npm run type-check

# Запуск на production сервере
npm start
```

## Решение проблем

### Бот не отвечает
- Проверьте токен в .env
- Убедитесь, что сервер запущен
- Проверьте логи: `npm run dev`

### Веб-приложение не загружается
- Проверьте WEB_APP_URL в .env
- Убедитесь, что веб-приложение собрано (`npm run build` в папке game)
- В production проверьте HTTPS

### Данные не сохраняются
- Проверьте браузер консоль на ошибки
- Убедитесь, что localStorage доступен
- Для Telegram CloudStorage, используйте HTTPS

## Дополнительные ресурсы

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Web Apps](https://core.telegram.org/bots/webapps)
- [Telegraf документация](https://telegraf.js.org/)
- [React документация](https://react.dev)
- [Vite документация](https://vitejs.dev)

## Поддержка

Для вопросов и помощи откройте issue или обратитесь к команде разработки.
